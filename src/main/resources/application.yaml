server:
  port: 8000

services:
  url:
    core: http://localhost:8080
    core-frontend: http://localhost:5173
    admintool-core: http://localhost:8081
    admintool-frontend: http://localhost:5174
spring:
  cloud:
    gateway:
      httpclient:
        websocket:
          max-frame-payload-length: 655460
